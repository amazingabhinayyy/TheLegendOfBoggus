using System;
using System.Collections.Generic;
using System.Diagnostics.Metrics;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace Sprint2_Attempt3
{
    public class TransitionRooms
    {
        public TransitionRooms(currentRoom, nextRoom,....) 
        {
            counter = 0;
        }

        switch (direction) {
            case "north":
            {
                shift all things down;
            }
            case "east": 
            ////
        // playerblockhandler -- player door iswalkable collisions : call room changeto***room depending on which
        // in each room: changeto***room calls transitionRoom and passes in parameter of room to switch to

        //transitionRoom: shifting all objects down 
    }
}
