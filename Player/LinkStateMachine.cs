public class LinkStateMachine
{/*

    private enum linkDirection { UP, DOWN, LEFT, RIGHT};
    private linkDirection direction = linkDirection.RIGHT;
    private bool damaged = false;
    private bool attacking = false;
    private int damagedTimer = 0;
    //should be enum
    private bool item = false; 


    public void newSprite()
    {
        if(direction == linkDirection.UP)
        {
            if (attacking)
            {
                new UpAttackingLinkSprite();
            } else if (idle)
            {

            } else if (moving)
            {

            }
        }
    }
    public void goUp()
    {
        if (!attacking)
        {
            direction = linkDirection.UP;
            
        }
    }

    public void goDown()
    {
        if (!attacking)
            direction = linkDirection.DOWN;
    }

    public void goLeft()
    {
        if (!attacking)
            direction = linkDirection.LEFT;

    }

    public void goRight()
    {
        if (!attacking)
            direction = linkDirection.RIGHT;
    }

    public void getDamaged()
    {
        damaged = true;
        DamagedTimer = 60;
        //construct damaged link sprite
    }

    public void attack()
    {
        attacking = true;
        //construct attacking sprite based on 
    }

    public void Update()
    {
        //need to add stuff here for updates to draw sprite based on direction and whether or not they are attacking
        //damaged timer
    }*/
}
